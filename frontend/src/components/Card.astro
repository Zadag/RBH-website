---
import RBHImage from '../assets/RBH-Logo.png';
import TopImg from '../assets/Top_glowing.png'; 
import MidImg from '../assets/Mid_glowing.png';
import FillImg from '../assets/2724-lol-role-fill.png';
import BotImg from '../assets/adc_glowing.png';
import SuppImg from '../assets/Support_glowing.png';
import JGImg from '../assets/Jungle_glowing.png';
import goldImg from '../assets/gold.png';
import platinumImg from '../assets/platinum.png';
import diamondImg from '../assets/diamond.png';
import masterImg from '../assets/master.png';
import grandmasterImg from '../assets/grandmaster.png';
import challengerImg from '../assets/challenger.png';
import emeraldImg from '../assets/emerald.webp';

import { globalState } from '../stores/globalStore';


interface Props {
	player: string;
	role: string;
	rank: string;
	champs: string;
	wins: number;
	losses: number;
	team: string;
	elo: number;
	opgg: string;
}

const { player, role, rank, champs, wins, losses, team, elo, opgg } = Astro.props;

let champ1;
let champ2;
let champ3;

if (champs.includes(',')) {
  champ1 = champs.split(', ')[0];
  champ2 = champs.split(', ')[1];
  champ3 = champs.split(', ')[2];
}

let roleImg;

if (role === "Top") roleImg = TopImg;
if (role === "Jungle") roleImg = JGImg;
if (role === "Mid") roleImg = MidImg;
if (role === "Support") roleImg = SuppImg;
if (role === "Bot" || role === "ADC") roleImg = BotImg;
if (role === "Fill") roleImg = FillImg;

let rankImg;

if (rank.includes('Gold')) rankImg = goldImg;
if (rank.includes('Platinum')) rankImg = platinumImg;
if (rank.includes('Emerald')) rankImg = platinumImg;
if (rank.includes('Diamond')) rankImg = diamondImg;
if (rank.includes('Master')) rankImg = masterImg;
if (rank.includes('Grandmaster')) rankImg = grandmasterImg;
if (rank.includes('Challenger')) rankImg = challengerImg;

---

<li class="link-card">
	<!-- <select id={`dropdown-${player}`} data-player={player}>
		<option value="all">Select Captain</option>
		<option value="Sasha">Sasha</option>
		<option value="LostZero">LostZero</option>
		<option value="ChillSavior">ChillSavior</option>
		<option value="HumanFaith">HumanFaith</option>
		<option value="Frischy">Frischy</option>
		<option value="SComm">SComm</option>
		
	</select> -->
		<h2>
			{player}
		</h2>
		<h3>
			{wins} - {losses}  |  {elo} Elo
		</h3>
		<div class="champs">
			<img class="champ-img" src=`https://ddragon.leagueoflegends.com/cdn/img/champion/loading/${champ1}_0.jpg`/>
			<img class="champ-img" src=`https://ddragon.leagueoflegends.com/cdn/img/champion/loading/${champ2}_0.jpg`/>
			<img class="champ-img" src=`https://ddragon.leagueoflegends.com/cdn/img/champion/loading/${champ3}_0.jpg`/>
			<div class="role-rank">
				<img class="role" src={roleImg.src} />
				<img class="rank-img" src={rankImg.src} />
			</div>
		</div>
		<h3 class="disc">discord.gg/intotherabbithole</h3>
		<!-- <a href=`${opgg}` target="_blank">
			OPGG
		</a> -->
</li>
<style>
	.link-card {
		list-style: none;
		display: flex;
		flex-direction: column;
		align-items: center;
		padding: 1px;
		margin: 1rem;
		min-width: 1200px;
		min-height: 675px;
		background-color: #000000;
		background-image: none;
		background-size: 400%;
		border: solid;
		border-color: rgb(254, 91, 91);
		border-radius: 7px;
		border-width: 10px;
		background-position: 100%;
		background-image: url(../assets/RBH-Logo.png);
		background-size: 500px;
		background-repeat: no-repeat;
		background-position: center;
		transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		box-shadow: inset 0 0 5px 1px rgb(214, 174, 255), 1px 1px 5px 1px rgb(214, 174, 255);
	}
	h2 {
		margin: 0;
		font-size: 4rem;
		color: rgb(210, 81, 81);
		text-shadow: 1px 1px 12px rgb(196, 94, 94);
		transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
	}
	p {
		margin-top: 0.5rem;
		margin-bottom: 0;
	}
	.link-card:is(:hover, :focus-within) {
		/* background-position: 0;
		background-image: var(--accent-gradient); */
	}
	.link-card:is(:hover, :focus-within) h2 {
		/* color: rgb(var(--accent-light)); */
	}
	.champ-img {
		height: 70%;
		margin: 0.5rem;
	}
	.champs {
		height: 27rem;
	}
	.role {
		position: relative;
		bottom: 11rem;
		left: 37rem;
	}
	.rank-img {
		position: relative;
		bottom: 20rem;
		left: 29rem;
	}
  a {
		position: relative;
		bottom: 3rem;
		text-decoration: none;
		color: blue;
		font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
		font-size: 4rem;

	}
  .disc {
		position: relative;
		left: 20rem;
		bottom: 2rem;
		font-size: 2rem;
		color: rgb(210, 81, 81);
		text-shadow: 1px 1px 12px rgb(196, 94, 94);
		padding: 0px;
		margin: 0px;
		font-weight: 500;
	}
</style>
